{
  "name": "burak",
  "version": "1.0.0",
  "description": "\"This is burak backend project\"",
  "main": "src/server.ts",
  "scripts": {
    "start": "ts-node src/server.ts",
    "start:dev": "nodemon --exec ts-node src/server.ts",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "MIT",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.0",
    "moment": "^2.31.0"
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "nodemon": "^3.0.2",
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.2.2"
  }
}
